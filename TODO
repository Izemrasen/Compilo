Compiler
	Pointers
		=> align interpreter w/ compiler
	BUGS
		#1. a simple affectation creates a new entry in symtable!!!
		#2. If/else: when multiple "if" in "else", "if" jump is not patched
	Make use of "initialized" flag (error etc.)
	Support for strings???
	Implement print(void *x)!!!
	Support comments
	
	EXTRA MVP
		Optimisations
			Instead of passing arithmetic results via memory, pass it via registers
			Distribute registers to avoid using the same repeatedly
			Clear symbol table: rm anonym symbols, etc. (cf TODO)
			...
		Functions
		Instead of "sprintf; ...", create handy function
		instr_add(): labelise instructions for debugging purposes